/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./**/*.{html,css}"], // Include all HTML and CSS files in the root folder and its subdirectories
  theme: {
    extend: {},
  },
  plugins: [],
}
